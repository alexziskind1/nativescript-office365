<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo">

    <StackLayout>
        <ListView items="{{ children }}">
            <ListView.itemTemplate>
                <Label text="{{ 'item name: ' + name }}" />
            </ListView.itemTemplate>
        </ListView>
    </StackLayout>

</Page>